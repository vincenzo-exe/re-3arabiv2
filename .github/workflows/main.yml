name: Build

concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true

on:
  workflow_dispatch:
  push:
    branches:
      - master
      - main
    paths-ignore:
      - '*.md'

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Source
        uses: actions/checkout@v4
        with:
          path: "src"
          fetch-depth: 0

      - name: Checkout Builds Branch
        uses: actions/checkout@v4
        with:
          ref: "builds"
          path: "builds"

      - name: Setup JDK 17
        uses: actions/setup-java@v5.2.0
        with:
          distribution: "temurin"
          java-version: 17

      - name: Setup Android SDK
        run: |
          export ANDROID_HOME="${ANDROID_HOME:-$ANDROID_SDK_ROOT}"
          export PATH="$ANDROID_HOME/cmdline-tools/latest/bin:$ANDROID_HOME/platform-tools:$PATH"
          yes | sdkmanager --licenses

      - name: Remove comments from source files
        working-directory: "src"
        shell: python
        run: |
          import os
          import re

          pattern = re.compile(r'^\s*//.*\r?\n?', re.MULTILINE)

          for root, dirs, files in os.walk('.'):
              dirs[:] = [d for d in dirs if not d.startswith('.')]
              for file in files:
                  if file.endswith('.kt') or file.endswith('.kts'):
                      file_path = os.path.join(root, file)
                      try:
                          with open(file_path, 'r', encoding='utf-8', errors='ignore') as f:
                              content = f.read()
                          
                          cleaned_content = pattern.sub('', content)
                          
                          if cleaned_content != content:
                              with open(file_path, 'w', encoding='utf-8') as f:
                                  f.write(cleaned_content)
                              print(f"Removed comments from: {file_path}")
                      except Exception as e:
                          print(f"Error processing {file_path}: {e}")

      - name: Push cleaned files to source branch (Before Secrets Injection)
        working-directory: "src"
        run: |
          git config --local user.email "actions@github.com"
          git config --local user.name "GitHub Actions"
          git add -u
          if ! git diff --cached --quiet; then
            git commit -m "Strip comments [skip ci]"
            git push origin HEAD:${{ github.ref_name }}
          else
            echo "No changes to push"
          fi

      - name: Inject Secrets into Source Code
        working-directory: "src"
        shell: python
        env:
          RIFT_JWT_SECRET: ${{ secrets.RIFT_JWT_SECRET }}
          RIFT_FIREBASE_API_KEY: ${{ secrets.RIFT_FIREBASE_API_KEY }}
          RIFT_FIREBASE_APP_ID: ${{ secrets.RIFT_FIREBASE_APP_ID }}
          RIFT_ANDROID_PACKAGE: ${{ secrets.RIFT_ANDROID_PACKAGE }}
          RIFT_ANDROID_CERT: ${{ secrets.RIFT_ANDROID_CERT }}
        run: |
          import os

          replacements = {
              "RIFT_JWT_SECRET_PLACEHOLDER": os.getenv("RIFT_JWT_SECRET", ""),
              "RIFT_FIREBASE_API_KEY_PLACEHOLDER": os.getenv("RIFT_FIREBASE_API_KEY", ""),
              "RIFT_FIREBASE_APP_ID_PLACEHOLDER": os.getenv("RIFT_FIREBASE_APP_ID", "")
          }

          for root, dirs, files in os.walk('.'):
              for file in files:
                  if file.endswith('.kt') or file.endswith('.kts'):
                      file_path = os.path.join(root, file)
                      try:
                          with open(file_path, 'r', encoding='utf-8', errors='ignore') as f:
                              content = f.read()
                          
                          modified = False
                          for placeholder, secret_val in replacements.items():
                              if placeholder in content and secret_val:
                                  content = content.replace(placeholder, secret_val)
                                  modified = True
                          
                          if modified:
                              with open(file_path, 'w', encoding='utf-8') as f:
                                  f.write(content)
                              print(f"Injected secrets into: {file_path}")
                      except Exception as e:
                          print(f"Error processing {file_path}: {e}")

      - name: Build Plugins
        working-directory: "src"
        run: |
          echo "=== listing files in src folder ==="
          ls -la
          
          if [ -f "gradlew" ]; then
            echo "gradlew found, setting execution permissions..."
            chmod +x gradlew
            GRADLE_CMD="./gradlew"
          else
            echo "gradlew NOT found! Falling back to system gradle..."
            GRADLE_CMD="gradle"
          fi
          
          $GRADLE_CMD make makePluginsJson

          python3 -c "
          import json, urllib.request, os, urllib.parse
          generated_path = 'build/plugins.json'
          existing_builds_path = os.path.join(os.environ.get('GITHUB_WORKSPACE', ''), 'builds', 'plugins.json')
          url = 'https://raw.githubusercontent.com/${{ github.repository }}/builds/plugins.json'
          
          def get_cs3_filename(url_str):
              if not url_str:
                  return ''
              parts = url_str.split('/')
              unquoted = urllib.parse.unquote(parts[-1])
              if unquoted.endswith('.cs3'):
                  return unquoted[:-4]
              return unquoted
              
          builds_name_map = {}
          try:
              if os.path.exists(existing_builds_path):
                  with open(existing_builds_path, 'r', encoding='utf-8') as f:
                      builds_data = json.loads(f.read())
                  for item in builds_data:
                      cs3_name = get_cs3_filename(item.get('url'))
                      if cs3_name:
                          builds_name_map[cs3_name] = item.get('name')
          except Exception as e:
              print(f'Error reading local builds: {e}')
              
          remote_name_map = {}
          try:
              response = urllib.request.urlopen(url)
              remote_data = json.loads(response.read().decode('utf-8'))
              for item in remote_data:
                  cs3_name = get_cs3_filename(item.get('url'))
                  if cs3_name:
                      remote_name_map[cs3_name] = item.get('name')
          except Exception as e:
              print(f'Error reading remote: {e}')
              
          try:
              if os.path.exists(generated_path):
                  with open(generated_path, 'r', encoding='utf-8') as f:
                      gen_data = json.loads(f.read())
                  for item in gen_data:
                      int_name = item.get('internalName')
                      if int_name in builds_name_map:
                          item['name'] = builds_name_map[int_name]
                          print(f'Preserved existing builds name for {int_name} -> {builds_name_map[int_name]}')
                      elif int_name in remote_name_map:
                          item['name'] = remote_name_map[int_name]
                          print(f'Set name for new plugin {int_name} from main branch -> {remote_name_map[int_name]}')
                  with open(generated_path, 'w', encoding='utf-8') as f:
                      json.dump(gen_data, f, indent=4, ensure_ascii=False)
          except Exception as e:
              print(f'Error merging names: {e}')
          "

          python3 -c "
          import json, os
          repo_data = {
              'name': '3rabi عربي',
              'description': 'مشاهدة الافلام والمسلسلات تجربة خالية من الاعلانات',
              'manifestVersion': 1,
              'pluginLists': ['https://raw.githubusercontent.com/${{ github.repository }}/builds/plugins.json']
          }
          repo_path = os.path.join(os.environ.get('GITHUB_WORKSPACE', ''), 'builds', 'repo.json')
          with open(repo_path, 'w', encoding='utf-8') as f:
              json.dump(repo_data, f, indent=4, ensure_ascii=False)
          "

          cp build/plugins.json $GITHUB_WORKSPACE/builds/plugins.json || echo "No plugins.json found in build"
          find . -type f -name "*.cs3" -exec cp {} $GITHUB_WORKSPACE/builds/ \; || echo "No .cs3 files found"

      - name: Push builds
        working-directory: "builds"
        run: |
          git config --local user.email "actions@github.com"
          git config --local user.name "GitHub Actions"
          git add .
          if ! git diff --cached --quiet; then
            git commit --amend -m "Build $GITHUB_SHA" || git commit -m "Build $GITHUB_SHA"
            git push --force origin HEAD:builds
          else
            echo "No changes to push"
          fi
